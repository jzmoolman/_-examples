export const BackgroundCirle = ({radius, strokeWidth}) => (
    <circle 
        cx={0}
        cy={0}
        r= {radius}
        fill="yellow"
        stroke="black"
        strokeWidth={strokeWidth}
    />
)       